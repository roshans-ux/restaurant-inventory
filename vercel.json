{
  "buildCommand": "npx prisma db push --accept-data-loss && npm run build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/order-batch",
      "schedule": "*/5 * * * *"
    }
  ]
}
